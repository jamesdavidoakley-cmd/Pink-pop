{
  "name": "numbermoor",
  "private": true,
  "version": "0.1.0",
  "description": "Numbermoor — a wizard school of numbers. Potion Scales (equations as balance) and Broom Formations (arrays, factors, primes).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "node scripts/boot-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^2.1.8",
    "playwright-core": "^1.49.0"
  }
}
