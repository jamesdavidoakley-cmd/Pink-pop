{
  "name": "crash-test",
  "private": true,
  "version": "0.1.0",
  "description": "Crash Test — build it, test it, watch it fail, fix it. Basic engineering for Max.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "node scripts/boot-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "playwright-core": "^1.49.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
