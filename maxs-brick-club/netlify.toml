# Netlify: set "Base directory" to maxs-brick-club in the site settings (or deploy this folder on its own).
[build]
  command = "npm run build"
  publish = "dist"

[[headers]]
  for = "/fonts/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
